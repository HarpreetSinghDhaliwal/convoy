import { TripFeedScreen } from "@/modules/trips";

// Phase 01 replaces the old placeholder home screen — the trip board is now
// the actual landing screen. Account/KYC/legal moved to /account.
export default function Home() {
  return <TripFeedScreen />;
}
